import {main,flag,authorizeWrite,admin,need} from './shared';
main(async()=>{if(flag('help')){console.log('pnpm owner:bootstrap --apply --project-ref=REF. Reserva exclusivamente maxi.salsa@gmail.com y solicita su verificación real.');return;}authorizeWrite();const email='maxi.salsa@gmail.com';const{error}=await admin().rpc('service_auth',{action:'reserve_owner',payload:{email}});if(error)throw new Error(`No se pudo reservar al propietario: ${error.message}`);const app=new URL(need('APP_URL'));const response=await fetch(new URL('/api/auth/register',app),{method:'POST',headers:{'Content-Type':'application/json',Origin:app.origin},body:JSON.stringify({email,name:'PracTICAtecnica.com',group:'Administración',specialty:'3006',consent:true}),signal:AbortSignal.timeout(60000)});if(!response.ok)throw new Error(`Reserva creada, pero inscripción no completada (${response.status}). Revise APP_URL y despliegue; puede repetir sin crear otro propietario.`);console.log('Solicitud registrada para maxi.salsa@gmail.com. Complete la verificación, use la temporal recibida, cámbiela y configure Google Authenticator. No se confirma entrega ni se concede acceso sin esos pasos.');});
