import {main,flag,option,need} from './shared';
main(async()=>{if(flag('help')){console.log('pnpm mail:smoke --to=correo@dominio --send. Envía UN mensaje de prueba autorizado; sin --send simula.');return;}const to=option('to');if(!to||!/^\S+@\S+\.\S+$/.test(to))throw new Error('Indique --to=su-correo con buzón de prueba autorizado.');if(!flag('send')){console.log(`Simulación: un mensaje de prueba a ${to}. Añada --send solo con autorización del titular.`);return;}const response=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${need('RESEND_API_KEY')}`,'Content-Type':'application/json'},signal:AbortSignal.timeout(15000),body:JSON.stringify({from:need('MAIL_FROM'),to:[to],subject:'Prueba de correo · PracTICAtecnica.com',text:'Este es un mensaje de prueba solicitado por la administración de PracTICAtecnica.com. No contiene contraseñas ni concede acceso. Confirme su recepción, revise también spam/correo no deseado. Puede tardar unos minutos.'})});if(!response.ok)throw new Error(`Proveedor no aceptó el mensaje (${response.status}).`);const result=await response.json()as{id?:string};console.log(`Proveedor aceptó la prueba. ID: ${result.id??'no informado'}. Falta comprobar recepción; accepted no equivale a delivered.`);});
