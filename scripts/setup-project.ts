import {main,flag,authorizeWrite,need} from './shared';
main(async()=>{
 if(flag('help')){console.log('pnpm setup:project --apply --project-ref=REF. Configura Auth hospedado mediante Management API; no crea ni cobra proyectos.');return;}
 const{ref}=authorizeWrite(),app=new URL(need('APP_URL'));if(app.protocol!=='https:'&&app.hostname!=='localhost')throw new Error('APP_URL necesita HTTPS.');
 const response=await fetch(`https://api.supabase.com/v1/projects/${ref}/config/auth`,{method:'PATCH',headers:{Authorization:`Bearer ${need('SUPABASE_ACCESS_TOKEN')}`,'Content-Type':'application/json'},signal:AbortSignal.timeout(30000),body:JSON.stringify({site_url:app.origin,uri_allow_list:`${app.origin}/verificar,${app.origin}/recuperar,${app.origin}/confirmar-correo`,disable_signup:true,mailer_autoconfirm:false,password_min_length:12,mfa_totp_enroll_enabled:true,mfa_totp_verify_enabled:true,mailer_secure_email_change_enabled:true,mailer_otp_exp:3600,mailer_allow_unverified_email_sign_ins:false,mfa_max_enrolled_factors:1,mailer_notifications_password_changed_enabled:true,mailer_notifications_email_changed_enabled:true,mailer_notifications_mfa_factor_enrolled_enabled:true,mailer_notifications_mfa_factor_unenrolled_enabled:true,mailer_subjects_email_change:'Confirme su cambio de correo · PracTICAtecnica.com',mailer_templates_email_change_content:`<h2>Confirme su cambio de correo</h2><p>Abra el enlace y confirme el cambio. Necesita confirmar tanto el correo actual como el nuevo.</p><p><a href="${app.origin}/confirmar-correo#token_hash={{ .TokenHash }}">Confirmar cambio de correo</a></p><p>Si no solicitó este cambio, no lo confirme y contacte a PracTICAtecnica.com.</p>`})});
 if(!response.ok)throw new Error(`Supabase no aceptó la configuración (${response.status}). Revise permiso del token y configuración de Auth; no se asume éxito.`);console.log('Auth configurado: registro directo cerrado, confirmación requerida, MFA TOTP y redirecciones limitadas. La inscripción de la aplicación usa el servidor.');
});
