import {spawn} from 'node:child_process';
import {resolve} from 'node:path';
import {main,flag} from './shared';
const checks:[string,string[]][]=[['TypeScript',[resolve('node_modules/typescript/bin/tsc'),'--noEmit']],['ESLint',[resolve('node_modules/eslint/bin/eslint.js'),'.','--max-warnings','0']],['Pruebas',[resolve('node_modules/vitest/vitest.mjs'),'run','--reporter=default','--reporter=json','--outputFile=qa/local-test-results.json']],['Contenido',['--import','tsx',resolve('scripts/content-validate.ts')]],['Build',[resolve('node_modules/next/dist/bin/next'),'build','--webpack']]];
main(async()=>{if(flag('help')){console.log('pnpm release:check. Ejecuta TypeScript, lint, pruebas, contenido y build; no despliega ni declara producción validada.');return;}for(const[label,args]of checks){console.log(`Comprobando ${label}…`);await new Promise<void>((ok,fail)=>{const child=spawn(process.execPath,args,{stdio:'inherit',shell:false,env:process.env});child.on('error',fail);child.on('exit',code=>code===0?ok():fail(new Error(`${label} no pasó (${code}). Release detenida.`)));});}console.log('Verificación de código terminada. Recepción real, DNS/TLS, restauración y seguridad hospedada siguen como gates independientes.');});
